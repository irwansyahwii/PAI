// Generated by CoffeeScript 1.9.2
(function() {
  var ScreenBase,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  ScreenBase = (function() {
    function ScreenBase() {
      this.immersiveMode = bind(this.immersiveMode, this);
      this.destroy = bind(this.destroy, this);
      this.show = bind(this.show, this);
      this.hide = bind(this.hide, this);
      this.hideWithTransition = bind(this.hideWithTransition, this);
      this.init = bind(this.init, this);
      this.width = 1024;
      this.height = 600;
      this.mainLayer = new Layer({
        width: this.width,
        height: this.height,
        backgroundColor: 'white'
      });
      this.mainLayer.states.add({
        hidden: {
          opacity: 0
        },
        show: {
          opacity: 1
        }
      });
    }

    ScreenBase.prototype.init = function() {
      return this.mainLayer.center();
    };

    ScreenBase.prototype.hideWithTransition = function() {
      return this.mainLayer.states["switch"]("hidden");
    };

    ScreenBase.prototype.hide = function() {
      return this.mainLayer.states.switchInstant("hidden");
    };

    ScreenBase.prototype.show = function() {
      return this.mainLayer.states.switchInstant("show");
    };

    ScreenBase.prototype.destroy = function() {
      return this.mainLayer.destroy();
    };

    ScreenBase.prototype.immersiveMode = function() {};

    return ScreenBase;

  })();

  module.exports = ScreenBase;

}).call(this);
